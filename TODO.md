## 大き目の機能
- [ ] パッケージの定義
  - パッケージ化して分割して、チェックに対するキャッシュをとる。
  - 各パッケージごとに必要な他のパッケージを宣言する。循環参照はなし。
  - visibility の話
- [ ] module ごとの差分を高速に調べて依存関係を洗いだすみたいな？
  - 差分がなかったらそのファイルの内容は無条件で信用していい。
  - `--full` で全部見て更新する
  - ちゃんと `pub` を考えた方がよさそう。
- [ ] LSP とかそこら辺の整備
  - これに対応するためには、 sema の前提を結構変更する必要がありそう。
- [ ] 証明支援系として、 `?` のある `.ref` を受け取ってそれを埋めれるだけ埋めるツールを作る。
  - auto とかできればいい。

## 構文
- CType をせっかく polymorphic にしたのに使ってないので使えるようにしたい。
  - そもそも `\CType` がないらしい。まあ使うかと言われたら使わないかもしれないが。
- equational reasoning とかそういうマクロを作りたい。
- law 周りをもっとどうにかできないか？「record の subset で law が示せるもの」みたいな定義で入れ子になっているのを楽に書きたい。
- 再帰関数書くために state, ready, acc, prec, precmatch みたいなのを並べているのをどうにかしたい。

### 検討中
- `#field{x}` はやっぱり dot 記法に合わせて `x #field` にしたい感じもある。パース上は `x #field{y}` は従来記法と断定して構わなくて、 `#field` のあとに `{` が来なかったら確定で新記法。問題は、 `f x #field` の書き方の場合で、これは ocaml とかの優先順位だと `f (x #field)` らしい。
- `\induction` も `\case` も各コンストラクタごとの分岐が `=>` なのがちょっとあってない。

## エラー表示周り
- vscode で見ると全体に赤線がひかれたりするのをもっとどうにかしたい。
  - ブロック内なら let の型定義が合わない時点でそこだけ赤線を引きたい
  - `?` をもっと有効活用して、そこに来るべき型を表示したい
  - そもそも、表示が kernel の項なので `(P: \Prop) -> (Q: \Prop) -> (_: ind(root.std.Logic.Proposition:1)[#1, #0]) -> ind(root.std.Logic.Proposition:1)[#1, #2]` みたいにちょっと生っぽい。

## コードのよくない点
- 関数定義で `\fun` が入れ子でインデントを入れすぎる。 300 行もあったり。
- block をそもそも全然使ってない。あと、 `\enough` も使ってない。
- Logic.And の入れ子も大量なので、 structure を使うようにする。
- ブロック内では `\takefrom x: A \by p;` を使うようにしたい。
