# ほしいもの
- metavariable?: Hole みたいなもの。型レベルでは infer の結果を入れて埋める。項レベルでは適当に constraint を生成して、ユーザーにそこに入るべき型を伝える。
- 型クラスとか trait: 多分 metavariable がないと厳しい気がする。やっていることは、値レベルから型を推測して、型と結びついた実装を見つける？
- type associated な item: コンストラクタ以外でも `Type#item` みたいな書き方をして、使える名前を増やす。
- マクロ: あまり自由にやらせずに、特殊なトークンを宣言してそれをもとに展開する。また、マクロ自体にも名前を付けて、特定のマクロだけ使えるようにする。
- package という区分: module とは別に、 rust でいう crate みたいなものがあったいい気がする。（コード上は `crateEnv` という名前になっている。）

## 構文
- module 内のつなげ方を `.` じゃなくて `::` にする。
- Record とか Struct の台集合は、 `\#` とかにする。
