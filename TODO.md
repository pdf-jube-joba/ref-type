## 大き目の機能
- [ ] 型クラスの実装
- [ ] パッケージの定義
  - パッケージ化して分割して、チェックに対するキャッシュをとる。
  - 各パッケージごとに必要な他のパッケージを宣言する。
  - visibility の話
- [ ] module ごとの差分を高速に調べて依存関係を洗いだすみたいな？
  - 差分がなかったらそのファイルの内容は無条件で信用していい。
  - `--full` で全部見て更新する
- [ ] LSP とかそこら辺の整備
  - これに対応するためには、 front の前提を結構変更する必要がありそう。
- [ ] 証明支援系として、 `?` のある `.ref` を受け取ってそれを埋めれるだけ埋めるツールを作る。
  - auto とかできればいい。

## コード内容の変更
- 定義が長いし一気に書きすぎてミスってる。記憶力とか一息に書く能力が高いが、普通にミスってるのでもっとこまめに書いてほしい。行数に制限を書ける？
- `()` をつけなくていいところにつけるのをやめる。
- `eqOfEqbTrue` とか `byCases` が内容同じでは？似たようなのがあるので、ちゃんと愚直に読んで二度手間を無くす。
- 関数定義で `\fun` が入れ子でインデントを入れすぎる。 300 行もあったり。
- `\exists Cast[] {}` をしなくていいように `\exists {}` の定義があるのでは？
- Sol の 5 high も括弧の数をめっちゃ間違える。なんで？
- やっぱ `\take` がすごい入れ子になる。調べると uniqueness の方はあまりないので、 `\take x: A \by term;` でも結構楽になりそう。ただ、 block をそもそも全然使ってない。
- Logic.And の入れ子も大量なので、 structure を使うようにする。
