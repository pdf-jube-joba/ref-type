[package]
name = "sema"
version = "0.1.0"
edition = "2024"

[dependencies]
project = { path = "../project" }
resolve = { path = "../resolve" }
syntax = { path = "../syntax" }
elaboration = { path = "../elaboration" }
serde = { version = "1.0.229", features = ["derive", "rc"] }
serde_json = "1.0.151"
sha2 = "0.11.0"

[lints.clippy]
too_many_arguments = "allow"

[build-dependencies]
sha2 = "0.11.0"
