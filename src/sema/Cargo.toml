[package]
name = "sema"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
raw = { path = "../raw" }
syntax = { path = "../syntax" }
elab = { path = "../elab" }
toml = "0.8.23"

[lints.clippy]
too_many_arguments = "allow"
