[package]
name = "resolve"
version = "0.1.0"
edition = "2024"

[dependencies]
syntax = { path = "../syntax" }

[lints.clippy]
too_many_arguments = "allow"

[dev-dependencies]
project = { path = "../project" }
