\module ContextualUnsolvedGoal(A: \Set(0)) {
  /* The reported goal should retain x in its local context. */
  \definition pending: A -> A := (x: A) => ?;
}
/* expect-error: unsolved metavariable goal */
