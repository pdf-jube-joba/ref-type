/* run rejects a term that is not a proof of its exact termination predicate. */
\module WrongRunCertificate(
  A: \Type,
  B: \Type,
  f: A -> \RunStep(A, B),
  a: A
) {
  \definition bad: B := \run(A, B, f, a, a);
}
