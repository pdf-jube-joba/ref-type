/* Acc is indexed by a reflected state, not by a compute term directly. */
\module AccWrongState(
  A: \Type,
  B: \Type,
  f: A -> \RunStep(A, B),
  a: A
) {
  \definition bad: \Prop := \Acc(A, B, f, a);
}
