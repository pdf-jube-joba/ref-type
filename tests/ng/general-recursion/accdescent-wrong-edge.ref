/* accdescent rejects an unrelated equality proof. */
\module AccDescentWrongEdge(
  A: \Type,
  B: \Type,
  f: A -> \RunStep(A, B),
  a: \RfType(A),
  b: \RfType(A),
  p: \Acc(A, B, f, a),
  wrong: a = a
) {
  \definition bad: \Acc(A, B, f, b) :=
    \accdescent(A, B, f, a, b, p, wrong);
}
