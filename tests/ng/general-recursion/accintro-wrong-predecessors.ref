\module AccIntroWrongPredecessors(
  A: \Set, B: \Set,
  f: A -> \RunStep(A, B),
  a: A,
  wrong: \Prop
) {
  \definition bad: \Acc(A, B, f, a) :=
    \accintro(A, B, f, a, wrong);
}
/* expect-error: ty, inferred_ty not convertible */
/* expect-error: check accessibility predecessors */
