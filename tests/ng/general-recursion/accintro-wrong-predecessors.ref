/* accintro rejects a premise with the wrong predecessor relation. */
\module AccIntroWrongPredecessors(
  A: \Type,
  B: \Type,
  f: A -> \RunStep(A, B),
  a: \RfType(A),
  wrong: \Prop
) {
  \definition bad: \Acc(A, B, f, a) :=
    \accintro(A, B, f, a, wrong);
}
