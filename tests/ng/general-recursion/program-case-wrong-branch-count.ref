\module IncompleteProgramCase {
  \inductive Bit: \VType :=
  | zero: Bit
  | one: Bit
  ;
  \definition bad: (Bit ~> \F(Bit)) :=
    (\cfun (x: Bit) => \match (x) \in Bit \with {
      | zero => \return(Bit::zero)
      });
}
/* expect-error: Program case must have one ordered branch per constructor */
