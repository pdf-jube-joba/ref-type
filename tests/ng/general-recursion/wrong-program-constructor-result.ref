\module WrongProgramConstructorResult {
  \inductive Swap(A, B: \VType): \VType :=
  | wrong: Swap[B, A];
  ;
}
/* expect-error: Program constructor wrong must return Swap with all datatype parameters */
