\module IncompleteProgramCase {
  \inductive Bit: \VType :=
  | zero: Bit;
  | one: Bit;
  ;
  \definition bad: \CFun(Bit, \F(Bit)) :=
    \clam(x, Bit,
      \vcase(Bit, x) {
      | zero() => \return(Bit#zero);
      });
}
