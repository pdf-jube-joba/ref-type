\module CertificateForWrongStep(
  A: \VType, B: \VType,
  first: \U(\CFun(A, \F(\RunStep(A, B)))),
  second: \U(\CFun(A, \F(\RunStep(A, B)))),
  initial: A,
  termination: \Acc(A, B, first, \RfTerm(A, initial))
) {
  \definition bad: \F(B) :=
    \run(A, B, second, initial, termination);
}
