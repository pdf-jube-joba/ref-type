\module WrongRunCaseInvariant(
  A: \VType, step: \U((A ~> \F(\RunStep[A, A]))), a: A,
  termination: \Acc[A^, A^](step^, a^)
) {
  \definition rejected: \F(A) :=
    \runCase[A, A](step, a, \return(\finish[A, A](a)))
      \by { accessibility: termination, equality: \refl(\finish[A^, A^](a^)) };
}
/* expect-error: check transition equality proof */
