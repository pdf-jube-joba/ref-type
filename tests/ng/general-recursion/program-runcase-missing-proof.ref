\module MissingRunCaseProof(A: \VType, step: \U((A ~> \F(\RunStep(A, A)))), a: A) {
  \definition rejected: \F(A) := \runCase(A, A, step, a, (\force(step)) a);
}
/* expect-error: expected keyword \by */
