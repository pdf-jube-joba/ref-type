\module WrongInitial(
  A: \VType, B: \VType,
  f: \U(\CFun(A, \F(\RunStep(A, B)))),
  a: A, b: B,
  termination: \Acc(A, B, f, \RfTerm(A, a))
) {
  \definition bad: \F(B) := \run(A, B, f, b, termination);
}
