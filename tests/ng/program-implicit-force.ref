/* expect-error: Program function values require an explicit \force before application */
\module ProgramImplicitForce {
  \inductive Bool: \VType := | false: Bool | true: Bool;
  \definition apply(f: \U(Bool ~> \F(Bool)), x: Bool): \F(Bool) := f x;
}
