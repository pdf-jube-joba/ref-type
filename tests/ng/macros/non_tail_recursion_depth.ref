\module Loop(A: \Set(0), x: A) {
    \definition first: A -> A -> A := \fun (a: A) => \fun (b: A) => a;
    \macro forever(..r) := first x forever!{..r};
    \definition result: A := forever!{};
}
/* expect-error: Macro expansion exceeded depth 128 */
