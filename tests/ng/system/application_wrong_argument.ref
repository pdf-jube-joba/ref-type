/* dep elim requires the argument to have the product domain. */
\module ApplicationWrongArgument(
    A, B: \Set(0),
    f: A -> A,
    b: B,
) {
    \definition bad: A := f b;
}
/* expect-error: ty, inferred_ty not convertible */
/* expect-error: check argument type for application */
