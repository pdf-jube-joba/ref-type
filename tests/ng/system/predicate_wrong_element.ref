/* Pred(A, S, x) requires x to inhabit the stated ambient set A. */
\module PredicateWrongElement(
    A, B: \Set(0),
    subsetOfA: \Power(A),
    b: B,
) {
    \definition bad: \Prop := \Pred(A, subsetOfA, b);
}
/* expect-error: ty, inferred_ty not convertible */
/* expect-error: check element type */
