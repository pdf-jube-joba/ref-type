/* Set-valued Take checks the stated map's uniqueness proof. */
\module TakeSetWrongUniqueness(
    X, T: \Set(0),
    f, g: X -> T,
    existsX: \exists X,
    uniqueG: (x1: X) -> (x2: X) -> g x1 = g x2,
) {
    \definition bad: T :=
        \take (x: X) => f x \by (existsX, uniqueG);
}
