/* Identity elimination checks the motive at the left endpoint. */
\module IdentityElimWrongBase(
    A: \Set(0),
    P: A -> \Prop,
    Q: \Prop,
    a, b: A,
    ab: a = b,
    q: Q,
) {
    \definition bad: P b :=
        \idelim(a = b \with x: A => P x) \by (q, ab);
}
