/* The current identity type is restricted to elements of Set(i). */
\module EqualityRejectsProofs(
    P: \Prop,
    p: P,
) {
    \definition bad: \Prop := p = p;
}
/* expect-error: equality carrier is not Set(i) */
/* expect-error: rule: "Equal" */
