/* Exists is formed over a set, not over a proposition. */
\module ExistsRequiresSet(
    P: \Prop,
) {
    \definition bad: \Prop := \exists P;
}
/* expect-error: set is not of Set(i) */
/* expect-error: rule: "Exists" */
