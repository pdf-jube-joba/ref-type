/* Power is formed only from a set, not from a proposition. */
\module PowerSetRequiresSet(
    P: \Prop,
) {
    \definition bad: \Set(0) := \Pow (P);
}
/* expect-error: set is not of Set(i) */
/* expect-error: rule: "PowerSet" */
