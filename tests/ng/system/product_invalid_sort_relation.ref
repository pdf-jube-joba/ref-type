/* The PTS relation contains no product from a proposition into a set. */
\module ProductInvalidSortRelation(
    P: \Prop,
    A: \Set(0)
) {
    \definition bad: \Set(0) := (p: P) -> A;
}
/* expect-error: no sort relation for product */
/* expect-error: rule: "Prod" */
