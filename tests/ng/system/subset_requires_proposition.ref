/* The predicate of a subset must be proposition-valued. */
\module SubsetRequiresProposition(
    A: \Set(0),
) {
    \definition bad: \Power(A) := \Subset(x, A, A);
}
/* expect-error: fail universe lift */
/* expect-error: check predicate */
