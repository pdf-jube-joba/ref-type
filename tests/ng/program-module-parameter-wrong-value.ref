/* expect-error: Program value types do not unify */
\module Source(X: \VType, x: X) {}
\module Bad {
  \inductive Left: \VType := | left: Left; ;
  \inductive Right: \VType := | right: Right; ;
  \import \root.Source(X := Left, x := Right::right) \as S;
}
