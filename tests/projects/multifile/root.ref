\module Foundation;

\module Consumer {
  \import \root.Foundation() \as F;
  \import \root.Foundation().Child() \as C;
  \definition identity: \forall (X: \Set) -> \Set := F.identity;
  \definition childIdentity: \forall (X: \Set) -> \Set := C.identity;
}
