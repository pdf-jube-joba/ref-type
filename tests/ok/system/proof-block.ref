/* Judgement-level premises are discharged by a trailing, context-aware block. */
\module DeferredProof(A: \Set) {
  \definition reflexive(x: A): x = x :=
    \Proof(x = x)
    proof {
      - goal (x: A): x = x := \refl(x);
    };
}
