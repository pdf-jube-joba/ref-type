\module MetaProgramTypeInference(A: \VType, a: A) {
  /* Program terms determine a metavariable expected type in their category. */
  \definition inferredValueType: _ := a;
  \definition inferredComputationType: _ := \return(a);

  \vcheck a: _;
  \ccheck \return(a): _;
}
