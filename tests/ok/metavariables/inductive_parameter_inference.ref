\module MetaInductive(A: \Set(0), a: A) {
  \inductive Box(X: \Set(0)): \Set(0) :=
    | box: X -> Box;
  ;

  \definition implicitBox: Box[A] := Box[_]#box a;
  \definition freshGoalBox: Box[A] := Box[?]#box a;
  \definition namedGoalBox: Box[A] := Box[?3]#box a;
}
