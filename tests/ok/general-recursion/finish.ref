/* A checked CBPV run reduces to return when its step finishes. */
\module RunFinish(
  A: \VType,
  B: \VType,
  a: A,
  b: B
) {
  \vdefinition step: \U(\CFun(A, \F(\PRunStep(A, B)))) :=
    \thunk(\clam(x, A, \return(\Pfinish(A, B, b))));
  \cdefinition result: \F(B) := \Prun(A, B, step, a);

  \ceval \Prun(A, B, step, a);
  \cnormalize \Prun(A, B, step, a);
}
