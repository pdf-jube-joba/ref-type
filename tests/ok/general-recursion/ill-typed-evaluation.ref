/* eval and normalize retain their original raw-term behavior when no run is present. */
\module IllTypedEvaluation() {
  \eval ((x: \SetKind(0)) => x) \Set(0);
  \normalize ((x: \SetKind(0)) => x) \Set(0);
}
