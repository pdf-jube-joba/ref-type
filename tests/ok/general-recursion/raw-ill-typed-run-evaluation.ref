/* Raw eval/normalize keep accepting a stuck, ill-typed Program expression. */
\module RawIllTypedProgramEvaluation(
  A: \VType,
  a: A
) {
  \ceval \capp(\return(a), a);
  \cnormalize \capp(\return(a), a);
}
