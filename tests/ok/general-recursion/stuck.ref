/* A well-typed open run remains unchanged when its step cannot expose a constructor. */
\module StuckRun(
  A: \Type,
  B: \Type,
  f: A -> \RunStep(A, B),
  a: A,
  termination: \Acc(A, B, f, \RfTerm(A, a))
) {
  \normalize \run(A, B, f, a, termination);
}
