\module ExplicitProofTerms(
  A: \Set,
  S: \Pow (A),
  x: A,
  refined: \Cast[A] (S),
){
  \definition inhabited: \exists A := \exact(x, A);
  \definition reflexive: x = x := \refl(x);
  \definition membership: \In[A] (S) (refined) := \bysub(A, S, refined);
}
