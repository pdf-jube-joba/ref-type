/* Local de Bruijn arguments must remain distinct during telescope instantiation. */
\module LocalParameterArguments(A: \Set(0)) {
    \inductive Tagged(first: A, second: A): \Set(0) :=
    | tag: Tagged;
    ;

    \definition local_constructor:
        (x: A) -> (y: A) -> Tagged[x, y] :=
        (x: A) => (y: A) => Tagged[x, y]::tag;

    \definition swapped_constructor:
        (x: A) -> (y: A) -> Tagged[y, x] :=
        (x: A) => (y: A) => Tagged[y, x]::tag;
}
