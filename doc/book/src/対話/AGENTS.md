CLI のやつ読みにくいので、ときどき考えをここにまとめてもらう。
- インライン数式は `\(` ~ `\)` 
- ディスプレイ数式は `\[` ~ `\]`
- 初めに、`## 体系定義` の中で構文定義/reduction定義/judgement定義 をする。
- 書き終わったら、過去の `{number}.md` を読み直して、すでに批判を受けた行為をしてないか確認する。
- 「○○はここには書かない。」のような、 "言及しないことに言及する"行為はしない。
