読むべき文章は `system.md`, `non-structural-recursion.md`, `fixpoint.md` で、 `doc/book/src/` 以下にある。

- 一般再帰関数は（すでに停止性が証明された）他の一般再帰関数を利用できること。
- 定義した一般再帰関数（停止性証明可能）が Set 側で利用できること。
- `run` は `run(f, a)` の `f` にも `a` にも Set 側特有の項（ take など）が来ないようにする。context に入っている variable が来た結果 `run(f, x)` が簡約できないことは許すが、基本的には close term であれば normal に持っていけるぐらいのことは示せてほしい。
- system.md に付け加えることができるぐらい、ちゃんと定義する。
