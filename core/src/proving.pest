PROVE_START = { "-" }
PROVE_END = { ";" }

exact_by_term = { "EXACT" ~ expression }
subset_elim_prop = { "SUBSET" ~ expression ~ "in" ~ expression ~ "<=" ~ expression }

PROOF = { PROVE_START ~ (
    exact_by_term | subset_elim_prop
) ~ PROVE_END }
